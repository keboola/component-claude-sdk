A highly configurable runner for the Claude Agent SDK inside Keboola. It lets you drive
agentic Claude workloads against your project data — supplying prompts, goals, models,
tools, MCP servers, permissions and runtime settings through component configuration — and
write the results back into Storage. The full configuration surface and behavior are
defined as the component is built; this description will be expanded once the design is
finalized.

